# Pending, NON-EXECUTED migrations

Files here are reviewed SQL drafts that MUST NOT be applied automatically.
They are kept outside `supabase/migrations/` so the Supabase migration
runner will not pick them up. Move a file into `supabase/migrations/` only
after explicit human approval.
